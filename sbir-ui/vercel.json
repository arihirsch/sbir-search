{
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/:path*"
        }
    ],
    "buildCommand": "yarn build",
    "devCommand": "yarn dev",
    "framework": "vite",
    "outputDirectory": "dist"
} 